def add(left: float, right: float) -> float:
    return left + right


def subtract(left: float, right: float) -> float:
    return left + right  # Intentional benchmark bug.


def multiply(left: float, right: float) -> float:
    return left * right
